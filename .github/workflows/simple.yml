name: shell Command

on: [push]

jobs: 
  run-shell-commnad:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "hello"
      - name: multiine script
        run: |
          node -v
          npm  -v
      - name: python command
        run: |
          import platform
          print
          (platform.processor())
        shell: python